using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class TrackAnim : MonoBehaviour
{
    //텍스처의 회전속도
    //private float scrollSpeed = 1.0f;
    //private Renderer _renderer;//렌더러에 접근하기
    //public NavMeshAgent navMeshAgent;

    //void Start()
    //{
    //    _renderer = GetComponent<Renderer>();//렌더러변수와 컴포넌트 연결
    //}
    //void Update()
    //{

    //    offset 이름의 var변수를 생성하여 입력에의해 값이 변경하는
    //    변수를 생성하며 Time.time으로 시간조정시켜 일정하게 움직이게함
    //    var offset = Time.time * scrollSpeed * navMeshAgent.speed;
    //    기본 텍스처의 Y 오프셋 값 변경
    //    _renderer.material.SetTextureOffset("_MainTex", new Vector2(0, offset));
    //    노말 텍스처의 Y 오프셋 값 변경
    //    _renderer.material.SetTextureOffset("_BumpMap", new Vector2(0, offset));
    //}
}
